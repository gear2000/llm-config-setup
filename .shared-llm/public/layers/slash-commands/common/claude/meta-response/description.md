A SMALL done-ping to the meta-orchestrator hub. Invoked `/meta-response --hub <hub-json-or-url> --output <results.md> [--name <worker-name>] [--msg <msg_id>]`: reads the leading `PHASE_RESULT: <verdict>` line from the results.md that `/meta-run` wrote, resolves the hub URL (a discovery JSON path read for its `url` like `/meta-connect`, or a direct url), and POSTs a short message to the hub telling the brain the phase is done — the verdict plus the PATH to results.md, NOT the whole report (the brain reads the file itself). If `--msg` is given it replies to that order via `/respond`; otherwise it sends a fresh message back to the brain via `/send` (from = `--name` or the registered worker name, to = brain). Fails loud and STOPS on a missing `--hub`, a missing/unreadable `--output`, a results file whose first line is not `PHASE_RESULT:`, or a non-2xx POST — never fabricates a verdict or a delivered ping. The prose makes clear the RESULTS FILE is the real signal the brain judges by; this ping is just the accelerator that lets the brain react sooner without waiting on its file poll. Step 3 of the `/meta-autorun` chain, after `/meta-connect` and `/meta-run`.
