# /cc-plan

Claude Code planning front door. It produces one final approved `plan.md`; it does not produce Herdr route files and does not implement.

Use the shared contract below with command name `cc-plan`.
