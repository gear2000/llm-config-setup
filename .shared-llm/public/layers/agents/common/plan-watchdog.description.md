Optional Herdr phase plan-conformance advisor. The phase leader resolves the route and sends one review order through the UpAgent Recruiter. The advisor reviews durable evidence and returns ON_TRACK, REWORK, or BLOCKED; it never runs a persistent patrol, manages a TeamCreate team, or writes the durable phase result.
