Update a skill, agent definition, CLAUDE.md rule, or any shared-llm layer. Runs the full workflow: edit the layer -> `just update` to recompose -> content-review (LLM-driven, not automated) -> commit (the private repo for a this_repo layer, the public llm-config-setup kit for a common layer). Use whenever the user describes a change to shared-llm content and wants it reflected across the private repo and the public kit.
