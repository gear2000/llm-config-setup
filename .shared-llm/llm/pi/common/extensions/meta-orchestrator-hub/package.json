{
  "name": "meta-orchestrator-sdk-leader",
  "private": true,
  "type": "module",
  "description": "Scoped deps for sdk-leader.ts only. The Pi worker half (index.ts + the reused modules) stays dependency-free; only the SDK leader needs node_modules.",
  "bin": {
    "meta-cc-client": "./meta-cc-client.ts"
  },
  "scripts": {
    "build": "node --experimental-strip-types --check meta-cc-client.ts && node --experimental-strip-types --check meta-plan-schema.ts && node --experimental-strip-types --check meta-plan.ts && node --experimental-strip-types --check index.ts",
    "test": "node --experimental-strip-types meta-plan-schema.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.183",
    "zod": "^4"
  }
}
