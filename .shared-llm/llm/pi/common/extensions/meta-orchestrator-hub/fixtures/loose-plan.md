# tiny feedback form app across Claude Code and Pi Harness

Goal: build a very small feedback form app.

## Phase 0 — build MVP

- Build the smallest useful version of the app.
- Include a simple form, client-side validation, and a submit summary.

Done:

- required-field validation prevents empty submissions;
- submit produces a visible summary.

## Route profile sketch

- Claude Code should do the initial implementation.
- Pi Harness should do the second-pass verification.
