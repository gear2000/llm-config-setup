Check whether a canonical meta plan is runnable before semi-AFK execution. Invoked `/meta-plan-check <plan.md> [route.yaml]`, it validates the two-file schema: clean `plan.md` plus route profile with `llm_profiles`, worktree branch template, finalization checks, per-phase `merge_back_at`, and five stage entries with `llm_profile` + `agent`. Use before `/meta-cc`, Meta-ORCH workers, or Meta-Herdr.
