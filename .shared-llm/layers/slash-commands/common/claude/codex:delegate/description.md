Hand a routine substantive coding task to Codex CLI as a peer subagent. Same underlying runtime as /codex:rescue, but framed as normal delegation rather than emergency rescue. Use when you want Codex to handle a piece of work (implementation, refactor, investigation, second-opinion review) in the course of normal flow — Claude is NOT stuck, the user is intentionally choosing Codex for this slice.
Triggers on phrases like "delegate to Codex", "let Codex handle this", "have Codex do X", "use Codex for Y", "give this to Codex", "hand this to Codex", or when the user invokes /codex:delegate directly. If Claude is actually stuck or the work is an emergency, prefer /codex:rescue instead — it shares the same runtime but signals different intent and may use different prompting.
