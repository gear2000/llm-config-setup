Claude Code-side meta runner entrypoint. Consumes a checked runnable `plan.md + route.yaml` pair with centralized `llm_profiles` and inline `agent` names. Fails before semi-AFK execution if `/meta-plan-check` would fail. Keeps `cc-*` as front-door workflow commands rather than meta runners, then runs phases through the shared phase Lead Agent and five-stage worktree protocol. Stage 1/2 run on a temporary worktree branch, `merge_back_at` determines Stage 3/4/5 merge timing, and Stage 5 finalizes with cleanup, green checks, and log review. No Claude team mode and no nested delegation.
