Execute an implementation plan produced by /do-plan-and-grill. The plan dictates the roster — workers, specialists, and any optional agents are listed in the plan's "Team" section. Default transport is subagents (fresh context windows per call); pass --team to use TeamCreate (named members, lateral SendMessage, TMUX windows, team-pulse heartbeat). The team leader orchestrates and delegates — does not work directly, always dispatches a subagent for concrete work. Still enforces the live-deploy gate: tests pass, builds pass, deployed, verified live with clean logs. Invocation: /do-implement [--team] <path-to-plan.md>.
